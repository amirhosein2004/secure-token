# --- Security ---
# It's recommended to generate a new secret key for production
# Use: openssl rand -hex 32
SECRET_KEY=
SALT=secure_token_salt_2024

# --- Token Settings ---
DEFAULT_EXPIRATION_HOURS=24
MAX_TOKENS_PER_USER=10

# --- Logging ---
# Available levels: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO